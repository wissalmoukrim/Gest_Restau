{% extends 'base_dashboard.html.twig' %}

{% block title %}Commande #{{ commande.id }}{% endblock %}

{% block body %}
<div class="container mx-auto p-4 max-w-4xl">
    
    <!-- Header -->
    <div class="flex justify-between items-center mb-6">
        <div>
            <h1 class="text-2xl font-bold text-gray-800">Commande #{{ commande.id }}</h1>
            <p class="text-gray-600">{{ commande.date|date('d/m/Y H:i') }}</p>
        </div>
        <a href="{{ path('client_commandes') }}" 
           class="text-blue-600 hover:text-blue-800 font-bold">
            ← Retour à mes commandes
        </a>
    </div>
    
    <!-- Carte principale -->
    <div class="bg-white rounded-2xl shadow-lg overflow-hidden mb-6">
        
        <!-- Barre de progression -->
        <div class="bg-gray-100 p-4">
            <h3 class="font-bold text-gray-700 mb-3">📦 Suivi de votre commande</h3>
            
            <div class="flex justify-between items-center mb-2">
                {% set etapes = [
                    {etat: 'en_attente', icon: '⏳', label: 'En attente'},
                    {etat: 'en_preparation', icon: '👨‍🍳', label: 'En préparation'},
                    {etat: 'en_livraison', icon: '🚚', label: 'En livraison'},
                    {etat: 'terminee', icon: '✅', label: 'Terminée'}
                ] %}
                
                {% for etape in etapes %}
                <div class="text-center">
                    <div class="w-12 h-12 rounded-full mx-auto mb-2 flex items-center justify-center
                        {% if etape.etat == commande.statut %}
                            bg-blue-600 text-white
                        {% elseif loop.index0 < array_search(commande.statut, ['en_attente', 'en_preparation', 'en_livraison', 'terminee']) %}
                            bg-green-600 text-white
                        {% else %}
                            bg-gray-300 text-gray-500
                        {% endif %}">
                        <span class="text-xl">{{ etape.icon }}</span>
                    </div>
                    <p class="text-sm font-medium">{{ etape.label }}</p>
                </div>
                {% endfor %}
            </div>
            
            <!-- Barre de progression -->
            <div class="w-full bg-gray-200 rounded-full h-2 mt-4">
                {% set progress = 
                    commande.statut == 'en_attente' ? 25 :
                    commande.statut == 'en_preparation' ? 50 :
                    commande.statut == 'en_livraison' ? 75 : 100
                %}
                <div class="bg-green-600 h-2 rounded-full transition-all duration-500" 
                     style="width: {{ progress }}%"></div>
            </div>
            <p class="text-center text-sm text-gray-600 mt-2">Progression : {{ progress }}%</p>
        </div>
        
        <!-- Détails -->
        <div class="p-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                
                <!-- Info commande -->
                <div>
                    <h3 class="font-bold text-gray-800 mb-3">📋 Informations</h3>
                    <div class="space-y-3">
                        <div class="flex justify-between">
                            <span class="text-gray-600">Statut :</span>
                            <span class="font-bold 
                                {% if commande.statut == 'en_attente' %}text-blue-600
                                {% elseif commande.statut == 'en_preparation' %}text-yellow-600
                                {% elseif commande.statut == 'en_livraison' %}text-purple-600
                                {% else %}text-green-600{% endif %}">
                                {{ commande.statut|replace({'_': ' '})|title }}
                            </span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Total :</span>
                            <span class="text-xl font-bold text-green-600">{{ commande.total }} DH</span>
                        </div>
                        <div class="flex justify-between">
                            <span class="text-gray-600">Adresse :</span>
                            <span class="font-bold text-right">{{ commande.adresseLivraison }}</span>
                        </div>
                        {% if commande.notes %}
                        <div class="mt-3 p-3 bg-yellow-50 rounded-lg">
                            <p class="text-sm text-gray-600">Note :</p>
                            <p class="text-sm">{{ commande.notes }}</p>
                        </div>
                        {% endif %}
                    </div>
                </div>
                
                <!-- Équipe -->
                <div>
                    <h3 class="font-bold text-gray-800 mb-3">👥 Équipe assignée</h3>
                    <div class="space-y-4">
                        {% if commande.chef %}
                        <div class="flex items-center gap-3 p-3 bg-blue-50 rounded-lg">
                            <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center">
                                <span class="text-blue-600 text-xl">👨‍🍳</span>
                            </div>
                            <div>
                                <p class="font-bold">Chef {{ commande.chef.prenom }}</p>
                                <p class="text-sm text-gray-600">Prépare votre commande</p>
                            </div>
                        </div>
                        {% endif %}
                        
                        {% if commande.livreur %}
                        <div class="flex items-center gap-3 p-3 bg-green-50 rounded-lg">
                            <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center">
                                <span class="text-green-600 text-xl">🚚</span>
                            </div>
                            <div>
                                <p class="font-bold">Livreur {{ commande.livreur.prenom }}</p>
                                <p class="text-sm text-gray-600">Livrera votre commande</p>
                                {% if commande.livreur.telephone %}
                                <button onclick="appelerLivreur('{{ commande.livreur.telephone }}')"
                                        class="mt-1 text-sm text-green-600 hover:text-green-800">
                                    📞 {{ commande.livreur.telephone }}
                                </button>
                                {% endif %}
                            </div>
                        </div>
                        {% endif %}
                        
                        {% if not commande.chef and not commande.livreur %}
                        <div class="text-center p-4 bg-gray-100 rounded-lg">
                            <p class="text-gray-600">En attente d'assignation...</p>
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>
            
            <!-- Liste des plats -->
            <div class="mt-8">
                <h3 class="font-bold text-gray-800 mb-3">🍽️ Détails des plats</h3>
                <div class="border border-gray-200 rounded-lg overflow-hidden">
                    <div class="bg-gray-800 text-white p-3 font-bold grid grid-cols-4">
                        <div>Plat</div>
                        <div class="text-center">Quantité</div>
                        <div class="text-center">Prix unitaire</div>
                        <div class="text-center">Total</div>
                    </div>
                    {% for item in commande.commandeItems %}
                    <div class="grid grid-cols-4 p-3 border-b border-gray-100 hover:bg-gray-50">
                        <div class="flex items-center gap-2">
                            <span class="text-xl">🍛</span>
                            <div>
                                <p class="font-bold">{{ item.plat.nom }}</p>
                                <p class="text-sm text-gray-500">{{ item.plat.categorie }}</p>
                            </div>
                        </div>
                        <div class="text-center self-center">{{ item.quantite }}</div>
                        <div class="text-center self-center">{{ item.prixUnitaire }} DH</div>
                        <div class="text-center self-center font-bold text-green-600">
                            {{ item.quantite * item.prixUnitaire }} DH
                        </div>
                    </div>
                    {% endfor %}
                    <div class="bg-gray-100 p-3 flex justify-between items-center">
                        <span class="text-lg font-bold">Total général</span>
                        <span class="text-2xl font-bold text-green-600">{{ commande.total }} DH</span>
                    </div>
                </div>
            </div>
            
            <!-- Actions -->
            <div class="mt-8 flex gap-3">
                <a href="{{ path('app_plat_index') }}" 
                   class="bg-green-600 hover:bg-green-700 text-white px-6 py-3 rounded-lg font-bold transition">
                    Commander à nouveau
                </a>
                
                {% if commande.statut == 'en_livraison' %}
                <button onclick="suivreLivreur()"
                        class="border-2 border-blue-600 text-blue-600 hover:bg-blue-50 px-6 py-3 rounded-lg font-bold transition">
                    🗺️ Suivre la livraison
                </button>
                {% endif %}
                
                {% if commande.statut == 'en_attente' %}
                <form method="POST" action="{{ path('client_commande_annuler', {id: commande.id}) }}" 
                      onsubmit="return confirm('Annuler cette commande ?')"
                      class="ml-auto">
                    <button type="submit" 
                            class="bg-red-600 hover:bg-red-700 text-white px-6 py-3 rounded-lg font-bold transition">
                        ❌ Annuler la commande
                    </button>
                </form>
                {% endif %}
            </div>
        </div>
    </div>
    
    <!-- Timeline des événements (optionnel) -->
    <div class="bg-white rounded-2xl shadow-lg p-6">
        <h3 class="font-bold text-gray-800 mb-4">⏱️ Historique</h3>
        <div class="space-y-4">
            <div class="flex items-start gap-3">
                <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-blue-600">📝</span>
                </div>
                <div>
                    <p class="font-bold">Commande créée</p>
                    <p class="text-sm text-gray-600">{{ commande.date|date('d/m/Y H:i') }}</p>
                </div>
            </div>
            
            {% if commande.chef %}
            <div class="flex items-start gap-3">
                <div class="w-8 h-8 bg-yellow-100 rounded-full flex items-center justify-center flex-shrink-0">
                    <span class="text-yellow-600">👨‍🍳</span>
                </div>
                <div>
                    <p class="font-bold">Assignée au chef {{ commande.chef.prenom }}</p>
                    <p class="text-sm text-gray-600">Préparation en cours</p>
                </div>
            </div>
            {% endif %}
        </div>
    </div>
</div>

<script>
function appelerLivreur(telephone) {
    if (telephone && telephone !== 'N/A') {
        if (confirm('Appeler le livreur au ' + telephone + ' ?')) {
            window.location.href = 'tel:' + telephone;
        }
    } else {
        alert('Numéro non disponible');
    }
}

function suivreLivreur() {
    alert('Fonction de suivi en temps réel (serait intégrée avec Google Maps)');
    // Ici, vous pourriez intégrer une vraie carte
    // window.open('https://maps.google.com/?q=' + adresse);
}

// Actualiser la page toutes les 30 secondes pour les mises à jour
setTimeout(() => {
    window.location.reload();
}, 30000);
</script>
{% endblock %}